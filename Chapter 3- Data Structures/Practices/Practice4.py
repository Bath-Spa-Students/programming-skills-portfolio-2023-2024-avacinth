numbers1 = [i for i in range(100)]  #List with 100 elements
numbers2 = []  # Empty list

# Copying the values from numbers1 to number2 using the copy() method
numbers2 = numbers1.copy()

# Showing that elements of numbers1 was copied to numbers2
print(numbers2)